# Copiar este archivo a .env y completar con tus valores reales
# IMPORTANTE: .env NO debe subirse a Git (ya está en .gitignore)

# Clave secreta para Flask (generar una nueva con: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=dev-secret-key-change-in-production

# Entorno
FLASK_ENV=production
FLASK_DEBUG=False

# Puerto (puede ser configurado por el hosting)
PORT=5000
